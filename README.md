# Lead Gen Pipeline

```bash
git clone https://github.com/mikey21-web/leadgenpipeline.git
cd leadgenpipeline
pip install -r requirements.txt
python -m uvicorn api:app --reload --port 8000
```

Then open http://localhost:8000
